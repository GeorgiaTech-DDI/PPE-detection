[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
 
[project]
name = "PPE-Detection"
version = "0.1.0"
 
[tool.setuptools.packages.find]
include = ["common", "goggle_detection", "face_detection"]